# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Calculator(By RDM)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Calculator(By RDM)

 pod 'SnapKit'

  target 'Calculator(By RDM)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Calculator(By RDM)UITests' do
    # Pods for testing
  end

end
